{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "运行 renderer",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/renderer.exe",
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build",
            "console": "internalConsole"
        }
    ]
}
